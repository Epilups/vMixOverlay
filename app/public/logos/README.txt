Drop team logo PNGs here (transparent background, roughly 200x200).
Reference them from the control panel as `logos/home.png`, `logos/away.png`.
A missing or blank path falls back to a striped placeholder cell in the overlay.
